/// @description Hier Beschreibung einfügen
state = "start";

x_pos_start = pos(x);
y_pos_start = pos(y) - 8;
x_pos_target = pos(x);
y_pos_target = pos(y);

x = apos(x_pos_start);
y = apos(y_pos_start);

speed_show = 0.1;

player_y_pos_retreat = 9;